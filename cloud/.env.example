PORT=4300
DATABASE_URL=postgres://user:password@host:5432/ozzbooks_cloud
# Signs session tokens — generate with `openssl rand -hex 32`. A leaked or
# weak secret here compromises every user's account.
OZZBOOKS_JWT_SECRET=
